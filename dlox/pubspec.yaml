name: dlox
description: An interpreters for Dlox languages.
version: 1.0.0
# homepage: https://www.example.com
environment:
  sdk: '>=2.14.0 <3.0.0'

publish_to: none

# dependencies:
#   path: ^1.8.0
dev_dependencies:
  lints: ^1.0.0
  dlox_generators:
    path: ../dlox_generators/
  build_runner:
  test: ^1.18.2
  mockito: ^5.0.16
dependencies:
  dlox_annotations:
    path: ../dlox_annotations/
  
